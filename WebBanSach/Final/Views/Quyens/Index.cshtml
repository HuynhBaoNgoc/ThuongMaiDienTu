@model IEnumerable<Final.Models.Quyen>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title" style="font-weight:800">User Role</h3>
                    <div class="box-tools">
                        <div class="input-group">
                            <div class="input-group-btn">
                                <a class="pull-right btn btn-sm btn-default" href="@Url.Action("Create","Quyens")"><i class="fa fa-plus"></i><span style="font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif"> Create</span></a>
                            </div>
                        </div>
                    </div>
                </div><!-- /.box-header -->
                <div class="box-body table-responsive no-padding">
                    <table class="table table-hover">
                        <tr style="font-family:sans-serif">
                            <th>@Html.DisplayNameFor(model => model.TenQuyen)</th>
                            <th></th>
                        </tr>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.TenQuyen)</td>
                                <td>
                                    <a class="btn btn-primary" href="@Url.Action("Edit","Quyens", new { id = item.MaQuyen })"><i class="fa fa-edit"></i></a>
                                    
                                    <a class="btn btn-danger" href="@Url.Action("Delete","Quyens", new { id = item.MaQuyen })"><i class="fa fa-trash"></i></a>
                                </td>
                            </tr>
                        }
                        </table>
                </div><!-- /.box-body -->
            </div><!-- /.box -->
        </div>
    </div>
</section><!-- /.content -->